services:
  - type: web
    name: signspeak-backend
    env: docker
    dockerfilePath: Dockerfile.backend
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
